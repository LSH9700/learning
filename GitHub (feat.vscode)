httpstagilog.tistory.com377

github에서 레포지토리 생성
1번경우 : readme 파일 생성
=> branch가 생성 => git clone 해야함
2번 경우 : readme 파일 생성 x
=> branch가 생성 x => git init 해야함

1번경우
git clone <주소.git>
git add <파일이름>
git commit -m "메세지"
git push 

2번 경우
작업 폴더로 진입
git init
git add <파일이름>
git remote add <별칭> <주소.git>
git commit -m "메세지"
git push -u <별칭> <branch>

branch 확인 명령어 : git branch
